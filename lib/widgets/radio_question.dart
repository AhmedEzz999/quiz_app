import 'package:flutter/material.dart';

class RadioQuestion extends StatelessWidget {
  const RadioQuestion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
